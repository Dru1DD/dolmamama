'use client';

import Image from 'next/image';

const foodSection = () => {
  return (
    <main
      className="relative w-full h-full flex flex-col 
    "
    >
      <section className="foodSection relative min-h-screen flex justify-center items-center bg-gray-100 shadow-2xl w-full">
        <div className="bg-white"></div>
      </section>
    </main>
  );
};

export default foodSection;
